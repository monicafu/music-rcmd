import React from 'react';

// Components
import Search from './Search';
import Login from './Login';

const Header = ( {} ) => {

	return (
		<header>
			<Search />
			<Login />
		</header>
	);
};

export default Header;